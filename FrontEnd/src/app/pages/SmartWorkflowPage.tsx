import { MarketingDetailPage } from '../components/MarketingDetailPage';

export function SmartWorkflowPage() {
  return (
    <MarketingDetailPage
      badge="Platform Feature"
      title="Smart Workflow"
      description="Automate attendance, shift assignment, and payroll approvals in one flow. Reduce manual entry errors and keep owner, clerk, and field worker operations synchronized."
      highlights={['Shift Automation', 'Attendance Validation', 'Payroll Readiness']}
    />
  );
}
